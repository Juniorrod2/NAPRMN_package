% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_PepsNMR.R
\name{plot_spectra}
\alias{plot_spectra}
\title{Plot NMR Spectra}
\usage{
plot_spectra(
  Spectrum_data,
  plot_resolution = 0.25,
  limit_n_points = F,
  plot_only = 0,
  Spectrum_window = NULL
)
}
\arguments{
\item{Spectrum_data}{Matrix or data frame containing NMR spectral data.
Rows represent samples and columns represent chemical shifts (ppm).
Must include a column named \code{"Sample"} if already converted to data frame.}

\item{plot_resolution}{Numeric (default = 0.25).
Defines the resolution used when bucketing (i.e., proportion of total points kept).}

\item{limit_n_points}{Logical (default = \code{FALSE}).
If \code{TRUE}, the spectra are bucketed using \code{PepsNMR::Bucketing}.
If \code{FALSE}, the full resolution is retained.}

\item{plot_only}{Integer vector or single integer (default = 0).
Specifies which samples to plot.
\itemize{
\item If \code{0}, the first 10 samples (or all, if <10) are plotted automatically.
\item If a single non-zero value is provided, that sample is plotted twice (internal handling).
\item If a vector is provided, the selected samples are plotted.
}}

\item{Spectrum_window}{Numeric vector of length 2 (default = \code{NULL}).
Chemical shift window for cropping the spectra (ppm).
Values are passed to \code{PepsNMR::WindowSelection(from.ws, to.ws)}.}
}
\value{
A \code{ggplot2} object containing the plotted spectra.
}
\description{
This function generates line plots for one or multiple NMR spectra stored in a
matrix or data frame format. It supports automatic selection of samples to plot,
optional bucketing, and cropping the spectrum to a defined chemical-shift window.
}
\examples{

\dontrun{
# Assuming `Spectra` is a matrix of NMR data with samples in rows:
plot_spectra(Spectra)

# Plot only samples 1, 3 and 5:
plot_spectra(Spectra, plot_only = c(1, 3, 5))

# Apply spectral window cropping:
plot_spectra(Spectra, Spectrum_window = c(0.5, 10))

# Limit number of points via bucketing:
plot_spectra(Spectra, limit_n_points = TRUE, plot_resolution = 0.1)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_PepsNMR.R
\name{overlaid_bins_plot}
\alias{overlaid_bins_plot}
\title{Plot Full Spectra With Overlaid Binned Intensities}
\usage{
overlaid_bins_plot(
  full_spectra,
  bin_data,
  bin_width,
  summary_function = mean,
  plot_only = 0,
  bin_round_precision = 4
)
}
\arguments{
\item{full_spectra}{Matrix or data frame containing full-resolution NMR spectra.
Passed to \code{plot_spectra()}. Rows represent samples; columns represent ppm values.}

\item{bin_data}{Matrix or data frame containing already-binned spectra.
Columns must correspond to bins, whose names represent bin centers (ppm).}

\item{bin_width}{Numeric. Width of each bin (in ppm) used for plotting the bar overlays.}

\item{summary_function}{Function applied across samples to summarize each bin
Useful summary functions: Mean; median; sum; sd; max; min.}

\item{plot_only}{Integer or vector
Samples to plot in the full spectra.
Passed directly to \code{plot_spectra()}.}

\item{bin_round_precision}{Integer. Number of decimal places used when rounding
Numeric precision used for plotting and attributing bins to metabolites}
}
\value{
A \code{plotly} interactive plot showing the full spectra and the overlaid bins.
}
\description{
This function creates an interactive plot combining raw NMR spectra
with overlaid binned spectral intensities.
The binned values are summarized across samples (default: mean) and displayed
as vertical bars on top of the full-resolution spectra.
}
\examples{
\dontrun{
# full_spectra: matrix of spectra (samples x ppm points)
# bin_data: binned spectra (samples x bins)

overlaid_bins_plot(
  full_spectra = my_full_spectra,
  bin_data = my_bin_matrix,
  bin_width = 0.05
)

# Using a custom summary function
overlaid_bins_plot(
  full_spectra = my_full_spectra,
  bin_data = my_bin_matrix,
  bin_width = 0.05,
  summary_function = median
)
}

}
